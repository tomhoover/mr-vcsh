##### read-only repos

#[src/github.com/jgoerzen/simplesnap]
#checkout = git clone 'https://github.com/jgoerzen/simplesnap.git' 'simplesnap'
#skip = ([ "$1" = update ] && ! hours_since "$1" 12)
#push = :
#fixups =
#    ln -sf "$MR_REPO/simplesnap" "/usr/local/sbin/simplesnap"
#    ln -sf "$MR_REPO/simplesnapwrap" "/usr/local/sbin/simplesnapwrap"
